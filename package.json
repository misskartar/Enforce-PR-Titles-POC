{
  "scripts": {
    "prepare:pr": "ncc build ./.github/actions/pr-title-checker/index.js -o ./.github/actions/pr-title-checker/dist --source-map"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.0"
  }
}
